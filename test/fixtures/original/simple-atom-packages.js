
const activationPromise = atom.packages.activatePackage("language-atom");

atom.packages.enablePackage("language-atom");

atom.packages.disablePackage("language-atom");

atom.packages.isPackageDisabled("language-atom");

atom.packages.isPackageActive("language-atom");

const path = atom.packages.resolvePackagePath("language-atom");

atom.packages.isPackageLoaded("language-atom");
